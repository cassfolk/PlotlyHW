////// Rejected Code

// var menuIndex = Object.entries(data).forEach(function(key, value){
    //     var row = menuIndex.append(key);
    //     console.log(row);
    // });

                                                // var menuIndex = data.filter(indexSelected => indexSelected.id === )

                                                // function getIndex(sample) {
                                                //     d3.json("samples.json").then((importedData) => {
                                                //     var data = importedData.data;
                                                    
                                                //     var menuBar = data.filter(something => something.index === sample); 
                                                    
                                                //     var somethingElse = menuBar[0];
                                                //     //Variables for dropdown
                                                //     // var names = importedData.names; // for dropdown menu
                                                // };
    

        // for (r=0; r < sampleObjects.length; r++) {
        //     var samplesRowId = sampleObjects[1]; 
        //     console.log("samplesRowId ", samplesRowId);
        // };


    // for (r=0; r<samplesRow.length; r++) {
        //     var sampleObjects = avoidNumbers(samplesRow);
        //     console.log("sampleObjects ", sampleObjects);
        //     var samplesRowId = sampleObjects.id; 
        //     // console.log("samplesRowId ", samplesRowId);
        //     // console.log("samplesRowId index ", sampleObjects[indexValue]);

        //                 // if (index === names.value() {
        //                 //     // Sample Variables to plot

        //                 //     console.log(samplesRowId);
        //                 //     // otuIds.push(getIntoRows(sampleObjects.otu_ids));
        //                 //     // sampleValues.push(getIntoRows(sampleObjects.sample_values));
        //                 //     // outLables.push(getIntoRows(sampleObjects.otu_labels));
        //                 // };
        // };



    // for (r=0; r<sampleObjects.length; r++) {
    //     var samplesRowId = sampleObjects.id; 
    //     console.log("samplesRowId ", samplesRowId);
    // };

    // var samplesRowId = samplesRow[0].id; 
    // console.log("samplesRowId ", samplesRowId);
    

    
    // var outIds = [];


    
    // for (i=0; i<samplesRow.length; 1++) {
    //     if (option === samplesRowId) {
    //         outIds.push(getIntoRows(samplesRow.sample_values));
    //     };
    // }



function avoidNumbers(row) {
    for (var i = 0; i < row.length; i++) {
        var info = row[i];
        return info;
    };
};


    // TRYING things for getIntoRows
    //return rows.map(function(row) {
    //                     // for (var i =0; i < row.length; i++) {
    //                     //     rows.map(function(row) {
    //                     //         return row[index];
    //                     //     });
    //                     // };
    //     return rows.map(function(row){
    //         return row[index];
    //     });
    //     // return row[index];
    // });



    // function clearDemo() {
//     d3.select("#sample-metadata").select("after");
// };

// clearDemo();

// d3.select("selDataset").on("change", optionChanged);

// need to id "optionChanged" in code somehow
// need to clear out demographic info




// var optionChanged = d3.select("select").selectAll("optionChanged").property("value");
// console.log(optionChanged);

        // var indexValue = names.findIndex(name => name === option);
        // console.log("index: ", indexValue);